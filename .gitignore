__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.venv/
venv/
env/

accounts_*.txt
mail_credentials.txt
*.txt
!requirements.txt
!mail_credentials.example.txt

cpa_auths/
screenshots/

config.json
config.local.json
vip0_mail.local.json
secrets/
.cloud_mail_domain_health.json
.cloud_mail_domain_health.json.tmp

# grok2api token pool (contains sso credentials)
token.json

# quota watch runtime state
.quota_watch_state.json
.quota_watch_state.json.tmp

# clash proxy node scoring
.clash_node_stats.json
.clash_node_stats.json.tmp

# per-domain registration health / demotion
.domain_health.json
.domain_health.json.tmp

# HTTP proxy list stats / local lists with credentials
.http_proxy_stats.json
all_proxies.txt
proxies.txt

*.log
.DS_Store
.idea/
.vscode/
DrissionPage/
autoPortData/
.browser_profiles/

# local deploy artifacts
.arts/

# MCP / browser test artifacts
.playwright-mcp/

logs/
# pool runtime / dead accounts
cpa_auths_dead/
databricks_auths/**
!databricks_auths/.gitkeep
databricks_auths_dead/
.pytest_dbx_tmp/

.domain_rr_index
pool_index.json
*.bak-sync*
grok_register_ttk.py.bak*
config.json.bak*
# local community dump (not for publish)
_community_ref/

# side OpenAI gateways (Kiro-Go binary + data/credentials; not Grok cpa_auths)
side_pools/
!side_pools/README.md

# local chatgpt2api clone + runtime (accounts.json / register.json / tokens)
chatgpt2api/
chatgpt_auths/
data/chatgpt_k12_*.json
data/chatgpt_register_mail_used.txt
data/k12_*.json
chatgpt_k12/config.local.yaml

# local import / community ref dumps (may contain tokens)
_import_*/
_sso_ref/
.proxy_health.json

# session-local imports / third-party clones / backups (do not commit)
k12_marely_import.json
k12_marely.json
backups/
_tools/cpa-auth-inspect/
_tools/authconv/

# temp scratch scripts
_tmp_*.ps1
_tmp_*.py

# quarantined community CPA dumps (thousands of account JSONs with tokens)
cpa_auths_quarantine/

# omk tooling internal state
.omk/
